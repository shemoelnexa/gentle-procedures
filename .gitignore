# Internal artefacts, not part of the shipped site
previews/

# Raw Citadel CGI render. Never shipped as-is (brand rule). The sanitised, cropped
# still derived from it, assets/img/clinic-arrival.jpg, IS tracked and shipped.
assets/img/reception.png

# OS junk
.DS_Store
Thumbs.db
desktop.ini
